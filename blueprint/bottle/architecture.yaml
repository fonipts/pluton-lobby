name: bottle
choices:
  - name: ""
dependencies:
  default:
  - bottle==0.12.25
script:
  pip_install:
    description: Install package
    command:
    - pip install -r requirements.txt
  start:
    command:
    - python main.py
files:
  default:
    - file: main.tpl
    - file: __init__.tpl
