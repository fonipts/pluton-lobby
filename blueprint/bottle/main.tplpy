from bottle import Bottle,run
from app.settings import routes

app = Bottle()


for value in routes:
    app.mount(value["url"], value["view_class"])

if __name__ == "__main__":
    app.run(host='localhost', port=8080)
